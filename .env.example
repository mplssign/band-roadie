# ============================================================================
# BandRoadie Environment Configuration
# ============================================================================

# -----------------------------------------------------------------------------
# FLUTTER APP (Mobile/Desktop)
# -----------------------------------------------------------------------------
# Copy this file to .env and fill in your Supabase credentials.
# The Flutter app reads SUPABASE_URL and SUPABASE_ANON_KEY.

SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Publishable Key (public "anon" key - safe for client apps)
# Find in: Supabase Dashboard > Settings > API > Project API keys
# Use the "anon public" key, NOT the service_role key.
SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# NEXT.JS WEB APP (Legacy - may be deprecated)
# -----------------------------------------------------------------------------

# Site URL (required for auth redirects)
# Dev: http://localhost:3000
# Prod: https://bandroadie.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase (Next.js uses NEXT_PUBLIC_ prefix for client-side access)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# NOTE: service_role key is for SERVER-SIDE ONLY (never in client apps)
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Spotify Web API (for BPM data)
# Create an app at https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Other environment variables...